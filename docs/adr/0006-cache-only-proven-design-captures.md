# Cache only revision-proven Design Captures

A Design Capture may be reused only when the Bridge Daemon proves the same authenticated plugin connection and a stable unchanged document revision. Missing or unstable revision evidence disables reuse; selection and named-section requests remain uncached until they can make the same proof.
