# Keep the MCP interface bounded and progressive

The MCP exposes a small stable tool set and serves workflows, Command Capability listings and large design sections on demand. We prefer parameters and progressive disclosure over new tools or oversized handshake prose because MCP context is a finite execution resource, not free documentation space.
